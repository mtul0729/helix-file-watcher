(#%require-dylib "libhelix_file_watcher" (only-in
    make-empty-watcher
    receive-paths!
    set-watched-files!
    watch-controller
))
(provide 
    make-empty-watcher
    receive-paths!
    set-watched-files!
    watch-controller
)
